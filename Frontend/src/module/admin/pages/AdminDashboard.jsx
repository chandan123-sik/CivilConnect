import React from 'react';

const AdminDashboard = () => {
    return (
        <div className="flex items-center justify-center h-full">
            {/* The user requested this page to be empty for now */}
        </div>
    );
};

export default AdminDashboard;
